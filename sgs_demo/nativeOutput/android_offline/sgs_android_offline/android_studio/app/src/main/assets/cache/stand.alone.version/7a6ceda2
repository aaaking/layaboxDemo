var __extends = (this && this.__extends) || (function () {
    var extendStatics = Object.setPrototypeOf ||
        ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
        function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var Loading = /** @class */ (function (_super) {
    __extends(Loading, _super);
    // public _mask:Laya.Image;
    // 	public _loadingImg:Laya.Image;
    // 	public _loadingLabel:Laya.Label;
    // 	public _loadingBottomLabel:Laya.Label;
    // 	public _loadingTopLabel:Laya.Label;
    function Loading() {
        return _super.call(this) || this;
    }
    Object.defineProperty(Loading, "instance", {
        get: function () {
            if (!this._instance)
                this._instance = new Loading();
            return this._instance;
        },
        enumerable: true,
        configurable: true
    });
    Loading.prototype.show = function (topStr, bottomStr) {
        Laya.stage.addChild(this);
        this.onResize(null);
        this.showAnim(true);
    };
    Loading.prototype.hide = function () {
        this.showAnim(false);
        this.removeSelf();
    };
    Loading.prototype.onResize = function (e) {
        if (e === void 0) { e = null; }
        this.width = Laya.stage.width;
        this.height = Laya.stage.height;
        this._mask.on(Laya.Event.CLICK, this, this.onTouch);
    };
    Loading.prototype.onTouch = function (e) {
    };
    Loading.prototype.showAnim = function (show) {
        var _this = this;
        if (show) {
            var costSeconds = 0;
            Laya.timer.loop(70, this, function () {
                _this._loadingImg.rotation += 10;
            });
            Laya.timer.loop(1000, this, function () {
                costSeconds += 1;
                var minute = Math.floor(costSeconds / 60).toString();
                if (minute.length == 1) {
                    minute = "0" + minute;
                }
                var second = Math.floor(costSeconds % 60).toString();
                if (second.length == 1) {
                    second = "0" + second;
                }
                _this._loadingLabel.text = minute + ":" + second;
            });
        }
        else {
            Laya.timer.clearAll(this);
        }
    };
    return Loading;
}(ui.loading.LoadingUI));
//# sourceMappingURL=Loading.js.map