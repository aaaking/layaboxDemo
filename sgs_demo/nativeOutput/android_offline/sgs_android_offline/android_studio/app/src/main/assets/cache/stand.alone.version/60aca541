var __extends = (this && this.__extends) || (function () {
    var extendStatics = Object.setPrototypeOf ||
        ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
        function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
/*
* name;
*/
var Exchange = /** @class */ (function (_super) {
    __extends(Exchange, _super);
    function Exchange() {
        var _this = _super.call(this) || this;
        _this._pageName.skin = "menu/logo_cards_exchange.png";
        return _this;
    }
    Object.defineProperty(Exchange, "instance", {
        get: function () {
            if (!this._instance)
                this._instance = new Exchange();
            return this._instance;
        },
        enumerable: true,
        configurable: true
    });
    Exchange.prototype.show = function (parent) {
        var _this = this;
        if (parent && !Exchange.instance.parent) {
            parent.addChild(Exchange.instance);
        }
        Dispatcher.on("userBalance", this, this.userBalance);
        Dispatcher.on("updateInfo", this, this.updateInfo);
        this._tab.selectedIndex = 4;
        ExchangeManager.instance.testInitCards(function () {
            _this._list.itemRender = ExchangeCard;
            // this._list.scrollBar.elasticDistance = 20;
            _this._list.renderHandler = new Laya.Handler(_this, _this.updateList);
            _this._list.array = ExchangeManager.instance.cards;
            // this._ui._list.array = ClubManager.myClubs;
            // this._list.mouseHandler = new Laya.Handler(this, this.selectList);
            _this.onResize(null);
        });
    };
    Exchange.prototype.updateInfo = function () {
        var _this = this;
        ExchangeManager.instance.testInitCards(function () {
            _this._list.array = ExchangeManager.instance.getCardsByCamp(_this._tab.selectedIndex + 1);
        });
    };
    Exchange.prototype.setList = function (camp) {
        this._list.array = ExchangeManager.instance.getCardsByCamp(camp);
    };
    /***渲染单元格时的回调方法***/
    Exchange.prototype.updateList = function (cell, index) {
        cell.updata();
    };
    Exchange.prototype.loading = function (success) {
        this._mask.visible = !success;
    };
    return Exchange;
}(CardList));
//# sourceMappingURL=Exchange.js.map